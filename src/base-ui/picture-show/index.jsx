import React, { memo } from "react";
import { PictureShowWrapper } from "./style";

const PictureShow = memo((props) => {
  const { pictures } = props;
  return <PictureShowWrapper>
   明天再写吧。。。
  </PictureShowWrapper>;
});

export default PictureShow;
